import { ShowRolesDirective } from './show-roles.directive';

describe('ShowRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowRolesDirective();
    expect(directive).toBeTruthy();
  });
});
